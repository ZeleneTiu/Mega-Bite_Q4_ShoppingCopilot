# Person C's reranker reads this. Copy to .env (gitignored) or export it.
# The graded run may have no network at all: with no key the agent
# short-circuits to retrieval-only and still scores 0.9092.
ANTHROPIC_API_KEY=
